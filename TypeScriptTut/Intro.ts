const character = "mario";

let age = 45;
let isRegister = false;

// const inputs = document.querySelectorAll("input");

// console.log(inputster);

// inputs.forEach((input) => {
//   console.log(input);
// });

const circ = (diameter: number) => {
  return diameter * Math.PI;
};

console.log(circ(5));
